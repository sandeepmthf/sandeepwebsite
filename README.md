# sandeepwebsite

  This is a code bundle for Create portfolio website. The original project is available at https://www.figma.com/design/e1OcIs1GTpU2rxQ37I6R2v/Create-portfolio-website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
